–ешение
совсем не об€зательно. переменные и без перезагрузки обновл€лись (по крайней мере у мен€). на самом деле алгоритм тут такой:
проверить, что при установке mingw не было ошибок;
проверить, что mingw установилс€ в папку C:\MinGW;
проверить, что переменна€ PATH реально изменилась. команда "set PATH" в консоли, откуда g++ запускаете... если пам€ть не измен€ет;
если каталога в PATH не указано, перезапустить консоль;
если после этого "set PATH" не показывает в списке c:\MinGW\bin, {
попробовать еще раз установить эту переменную, дл€ своего пользовател€;
перейти к п.3;
}, либо {
установить эту переменную в самой консоли:
 од
set PATH=C:\MinGW\bin;%PATH%
;
}
если после этого запуск команды "gcc" показывает
 од
"gcc" не €вл€етс€ внутренней или внешней
командой, исполн€емой программой или пакетным файлом.
{
// значит вы установили только msys, не установив компил€тор
return 'ƒоустановить MinGW';
}
если gcc показывает "gcc: no input files", а g++ показывает `echo ${сообщение из п.6} | sed 's/gcc/g++/'` {
// значит установили только компил€тор си, но не установили компил€тор с++
return 'ƒоустановить пакет g++ дл€ MinGW'; // он в отдельном пакете идет, обычно. ’от€ зависит от сборки MinGW
}
если команда g++ показывает "g++: no input files" {
// значит компил€тор работает, укажите какой файл нужно скомпилировать
return '¬се ќ ';
}
уточните свой вопрос, а пока вам не ответили, подумайте исчо и перейдите к п.3.
небольша€ нотка недетерминированности в вышеуказанном алгоритме позвол€ет реализовать свои творческие способности.

ƒобавлено через 11 минут
да, забыл еще, если вдруг команда gcc или g++ не выдает "no input files", а начинает выпендриватьс€ и считывать стандартный поток ввода, значит это стара€ верси€ mingw, скачайте поновее