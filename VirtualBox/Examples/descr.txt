Programs:
	- example(1).asm - перемножение 2-ух регистров;
	- example(2).asm - деление 2-ух регистров, с остатком;
	- example(3).asm - сравнение 2-ух регистров;
	- example(4.1).asm 
		                    \ 
		                      пример реализции безусловного перехода, или иначе зацикливания;
		                    /
	-  example(4.2).asm
	-  example(5).asm - последовательный вывод чисел в цикле, предшедствующих указанному
		                    (введенному с руки) числу (включая само, введенное, число);
	-  example(6).asm - аналогичная логика выполнения программы, что и в файле - "example(5).asm",
		                    с использованием меток;
	-  example(7).asm - вывод введенных пользователем чисел, в обратном порядке
		                    (по принципу - стэка);
	- example(8).asm -  вывод числа (из списка введенных от руки), 
		                    которое лежит по адресу - 44 (?);
	- example(9).asm - вывод содержимого регистра ah, с использованием функций;
	- example(10.1).asm 
		                    \
		                       программа, реализующая использование 2-ух байтовых данных,
		                       с последующим выводом на экран. В файле - example(10.2).asm еще также рассмотренно
		                       использование - space;
	 	                     /
	- example(10.2).asm
	    //Замечание!
	    //Прогрммы, описанные в файлах - example(10.1).asm/example(10.2).asm, используют адресацию памяти.
	- example(11).asm - программа, реализующая использование строковых данных,
		                       с последующим выводом на экран;
	- example(12).asm - программа поиска наибольшего числа;
	- example(13) - папка включающая в себя файлы: example(13).asm/data.asm, где example(13).asm - файл
		           с программой, полностью дублирующей логику программы в файле - example(12).asm,
		           а (подключаемый в example(13).asm) файл data.asm, содержит текстовые данные;
	- example(14).asm - программа деления вещественных чисел.
		



  